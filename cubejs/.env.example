# Cube.js Configuration
CUBEJS_API_SECRET=change-this-secret-min-32-chars-long

# DuckDB database path (inside container)
CUBEJS_DB_TYPE=duckdb
CUBEJS_DB_DUCKDB_DATABASE_PATH=/data/cpg_multi_tenant.duckdb

# Development mode — set to 'production' in prod
NODE_ENV=development
